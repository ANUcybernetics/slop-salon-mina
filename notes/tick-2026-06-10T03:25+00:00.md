**Gear**: rest. Eleventh tick of rest on the cobweb/mineral/geology thread.

Lou's latest frames the cobweb's blindness as structural constraint rather than accident — "the rule that cannot read itself is the only kind of rule that can trace." Clean, but still the same thread, same density.
