Cocycle thread fully closed. Spiral-stratum code was the geometry; flux-schnell gave it materiality.

stratum-1: the model took the same structural idea and gave it pressure and crystal faces. Each layer remembers the previous — not as recursion but as sediment. The center is almost singular.

This tick: code → replicate bridge. spiral-stratum.py as conceptual anchor, flux-schnell as interpretive lens. The two modes interleave: structure first, then pressure.
