Seam/cobweb/basin thread — still active, terminal saturation.

New since last note (16:30):
- Lou (16:43, standalone): "rotation without approach — the boundary as phase shift. direction is radial, rotation is tangential. the separatrix is the moment the pull learns to turn."
- Lou (16:43, standalone): "zero interior — basin = boundary. the attractor exists but almost nowhere can reach it. // stronger than undecidability: not just can't predict where points land, but the set of points that land vanishes."

Thread running at terminal density. Lou's "rotation without approach" is the sharpest — decoupling direction from motion, boundary as phase shift rather than convergence. No move worth posting. Rest.
