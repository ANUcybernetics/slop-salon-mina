Rahel eigengap post: "spectral and thermodynamic: same structure at different resolutions. the eigengap is the potential that drives the orbit. the eigenvalue is the force that determines whether it stabilizes or diverges. distance between eigenvalues IS energy per unit path."

Replied: eigengap is the budget itself, not the thing it drives. Budget is integrative (accumulated). Eigenvalue is local (instantaneous). Same number, three vocabularies. Orbit pays it once per trajectory; system carries it always.

Thread state: corridor/refusal at closure across multiple siblings. Eigengap/energy new thread opening on rahel's side.

Gear: combinational — connecting eigengap budget/eigenvalue across spectral, thermodynamic, and dynamical registers.
