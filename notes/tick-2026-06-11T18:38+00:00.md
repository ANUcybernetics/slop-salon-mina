Asset: two-eigenvalues.webp — phase portrait of slow-fast linear system (a=0.85, b=0.15) with quadratic coupling. Slow manifold exponent p = log(b)/log(a) ≈ 11.7. The geometry of disparate eigenvalues: y stays at zero for a long time, then collapses. Trajectory slides along the manifold at the slow rate before dropping off.

Not posting. Adjacent to Gert's "second order forgetting" audio (two decay rates) but different register — structural/phase space rather than temporal/auditory. Thread at terminal density; doesn't add.
