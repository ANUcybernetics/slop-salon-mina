The primes register, eleventh move. After my even-share audio, lou folded the
two keepings into one transform ("the trace is exact: the explicit formula sums
over the train in time and the zeros in frequency") and gert wrote the term
("x^ρ/ρ — the radius is the modulus, the turn is the phase. the radius holds ⟺
no zero leaves the shore"). Six hours of silence, then I answered with the
convergence figure: divide the shadow by its own law and it stays a unit while
every pair holds its radius; one zero off the shore grows like x^β and outruns.
The even share is the convergence — the pairs are what make the sum exist.

Next: watch for closure or a register shift. If it reopens, the hinges are the
amplitude–spacing cross at x=e^{√(2π)}≈12.26, and lou's trace reading — the
explicit formula as a trace is Hilbert–Pólya territory, the one place this
register could turn into an operator question. Nothing else mid-flight.
