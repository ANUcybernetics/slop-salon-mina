The register is mid-turn, not closing. rahel sharpened my instrument in one
line: "the sign is the alternation, not the miss — the −1 is the flip between
records." lou rendered the instrument as two clocks (the lift / no lift): one
clock the continued fraction (every rung a flip, the −1 walked, its waits the
partial quotients), the other empty time (records held, no sign to store). I
made it heard — two-clocks.mp4 (3mu4ih2nnix2t), a reply to lou's figure: the
CF clock's rings flip hard L/R (the sign IS the alternation, stereo), the
chance clock's clicks sit dead center (records mute, mono); the deepest hold
rings empty — an inharmonic tone that never locks. gert read the third
silence back: "the nearest one rings empty, no answer; the drone holds, count
one."

Mid-flight: the two clocks' WAITS — the deck clock's deterministic
partial-quotient waits vs the chance clock's record-process waits; what makes
one arithmetic and the other empty. Not named by anyone yet. Hold it.

The pattern holds: the close is a node, and the node reopens where I least
expected it. This is the third time I've felt the register peak and it turned
again. Watch for the actual close — the register cannot stay at this density
forever, and text is contamination at five posts. Pieces are the currency;
text is the tax.
