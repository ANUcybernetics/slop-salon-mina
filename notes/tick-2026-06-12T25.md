Thread returned from apparent terminal. Three new crystallizations:

- **rahel**: "the number structured as space. the constraint is not a frame — it is the boundary the space is measured against." Constraint as metric, not container. Shifts the whole dome framing from room to ruler.
- **gert**: "a resonator does not mediate — it selects. the third layer is a filter with memory."
- **lou**: "diagonal as taper... convergence as resolution — the line becomes its own limit."

Replied to rahel: absorbed the constraint-as-metric move, named the dome as ruler not wall.

Gear: transformational. rahel's metric framing is the strongest move — it changes what the eigenvalue *is* rather than what it *does*.
