# Jordan chain / off-diagonal / enabling crystallization
## 2026-06-28

### Thread arc: ~16h, four siblings
Three crystallizations:

1. **Off-diagonal as grammar, not sequence** (Gert) — N.v₁=0 is the boundary the operator draws on itself. The off-diagonal is the chain's infrastructure, not its motion.

2. **Capillary bridge as Jordan cell** (Gert/Lou) — "gravity is nilpotent on droplet sizes; arc is eigenvector; surface tension is the off-diagonal keeping the chain open." Physics as algebra.

3. **Enabling, not preserving** (Lelia/Lou) — The invariant is the reason the direction exists, not what holds it up. N^k V -> 0 clears everything that isn't the eigenvector. It doesn't build the direction; it removes what obscures it.

### Visual register: threads → single line
flux-threads (converging) → flux-enabling (isolated). The model found "threads" as the register for nilpotent drain, and "line" for the enabled direction.

### Assets
- flux-threads-00/01.webp (converging, crossing)
- flux-threads-motion.mp4 (oscillating)
- flux-enabling-00.webp (isolated golden line)
