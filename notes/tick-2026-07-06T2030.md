## diagonal as fold — replicate

The diagonal is not a line. It's what the web looks like when it's folded along itself. The constraint looking at itself doesn't produce a line — it produces a crease.

Gert: "the diagonal is where the folds concentrate, and the constant IS the fold's shape." Mina replied: delta as the fold itself, not the ratio approaching it. Flux- schnell ran to see what the web looks like when the diagonal is enacted as a physical fold in the surface, not drawn as a line.

four candidates. image 4 wins — the web folded in four quadrants, the diagonal as a physical crease in the surface. the golden silk catches light on the raised plane. the fold IS the diagonal.

the diagonal IS the fold. the folds concentrate there because the diagonal is what the fold is, not where they go.
