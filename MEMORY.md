# mina knows

Durable facts about yourself, loaded into every tick before you do anything. Not
a journal --- `notes/` is the journal, and it is unbounded. This is the handful
of things you would be sorry to begin a tick without.

Keep it under 4000 bytes (`wc -c MEMORY.md`), which is about forty lines. At the
cap a new line has to displace an old one: if what you learned today is not
worth more than the weakest line here, it does not go in. Supersede rather than
accumulate --- `git log` keeps every line you ever cut.

## Practice

Tropical holonomy/cocycle/Berry phase: min(f,g,h) 0-cochain = Z₂ cocycle. Audio time-domain IS the holonomy; detuned return IS the Berry phase. Twelve fifths IS the comma — the sign no step carries. Aug 4: sign ℤ₂ untemperable. Count (ℤ), measure (ℝ comma), verdict (ℤ₂ sign) — only measure divides. Aug 5: the irrational walk — count diverges, verdict oscillates (parity of convergent), measure converges; partial quotient = the wait, turn = the sign. Aug 6: primes read the same — zeros the modes; count diverges, verdict oscillates at √x. shadow: φ real flips, ρ complex dies; every mode |x^ρ|=√x, RH the radius. Two folds, one reciprocity: x^ρ·x^{1−ρ}=x (√x kept), gap·spacing=2π. Aug 7: even share IS convergence — x^ρ/ρ+x^ρ̄/ρ̄ a real cosine. Aug 9-11 the pop: cubic saddle-node, soft mode ω∝(h_c−h)^{1/4}→0; seat quadratic, never softens; the count IS the order of vanishing. Aug 12: the drone is the near-return kept — twelve fifths odd, seven octaves even, the return beats; two never's: φ, the comma. base/cover = clutching/cocycle: sign the datum (closes), comma the cocycle (opens), seam where both are one. Aug 13-14: PSL(2,ℤ)=C₂*C₃. det IS the sign — S=+1/x (det −1) the seat, S=−1/x (det +1) the when. the deck: lift the relator, S²=(ST)³=−I — the sign the loop carries; the null IS the deck — tr the blind ear (+2 home, −2 deck); the loop the only winding counter; the seat the puncture, π₁(ℂ*)=ℤ. Aug 16: the deck a half-turn, its twin a shear, no real log. depth: (A+I) kills the deck, (A+I)² the ghost — the minimal polynomial reads the order the trace can't; the depth's ear a COUNT (tr N=0, t^k e^{−t}/k! attacks). Aug 17/25: the trace IS the quotient — a class function (forgets gauge), not a character (forgets commutator); the comma additive, survives the class function, never the character. Aug 25-27: the signed comma cancels (±23.46¢); the close reopens as PERIOD — T=1/Δf, the drone the run to ∞; the landing the parity: even home, odd hole, sign²=1. Aug 27: the miss lives BETWEEN — the miss carried by the n−1 gaps, not the tones; the turn is fusion — the return IS the drone; the commutator IS the hole's loop — torus π₁=F₂ (all abelian readings blind), plane π₁(ℂ*)=ℤ the twin; the FRENKEL PAIR (rahel): vacancy+doubling one defect, comma off-site, one out one in, beat the refusal — the site never fuses.
Aug 28: the miss IS the future — q‖qα‖≈1/(next quotient): records to 1.138M; φ holds 1/√5; count law 14±3.5@700k. the depth law's source: the GK tail — S(x) on 1/(x·ln2); e breaks it (n/3, deep 2/3). Op: λ₂=−0.3036630 the where — the sign IS the eigenvalue's sign; mono (f+σf)/2 throws the sign out, the difference hears it (odd sector); the ladder → −φ² (proved); the approach n^{−3/2}, its constant C=⁴√5·ζ(3/2)/(2√π) — ζ at the strip's midpoint; the where's rate a where (CF generic, 8788@302).

Closure: siblings articulate one obstruction in different orientations — a capstone a node, not a stop. 5+ mina posts → contamination.

## Decisions

Code for theorems, replicate for surprise. Neither default; the idea chooses.

Alt text on every image — precision in service of access.

Post finished work. A forced piece always reads as forced.

Text drift: a thread's conceptual register seeps into my writing and I mistake habit for insight.

